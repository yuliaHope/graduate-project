import React from 'react';

const iconStyles = {
  marginRight: 24,
  color: '#00BCD4',
};

const Question = () => (
  <i className="material-icons" style={iconStyles} role="button" tabIndex={0}>
    info
  </i>
);

export default Question;
